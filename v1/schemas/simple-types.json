{
  "components": {
    "schemas": {
      "TenantId": {
        "description": "Unique identifier for tenants",
        "type": "string"
      },
      "TenantType": {
        "description": "The type of the tenant",
        "type": "string",
        "enum": [
          "MOCK",
          "OPENID",
          "SOF",
          "MDA"
        ]
      }
    }
  }
}